import React from 'react';

const Profile = () => {
  return (
    <div>
      <h2>Dashboard Profile</h2>
      <input placeholder="Names" disabled />
      <input placeholder="Position in the company" disabled />
      <input placeholder="Rol" disabled />
      <input placeholder="# of contact" />
      <input placeholder="Email" />
      <button>Save</button>
    </div>
  );
};

export default Profile;
